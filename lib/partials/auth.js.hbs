Client.Security = {}

{{#each security}}
{{#if (equal this.type "OAuth 2.0")}}
Client.Security.{{camelCase @key}} = function OAuth2 (options) {
  return new ClientOAuth2(extend({{{stringify this.settings}}}, options))
}
{{/if}}
{{/each}}
